# agent-helper 项目技术报告

> 生成时间：2026-09-01
> 分析范围：项目根目录 + `agent/` + `config/` + `loader/` + `tools/`（`tools/_legacy/` 为历史归档版本，`rocketmq_docker/` 按要求忽略）
> 代码规模：约 2 万行 Python（去重后有效模块约 60 个文件 / 13500 行，另有 `_legacy` 归档约 6600 行）

---

## 一、项目概述

`agent-helper` 是一套**文档智能解析与合规审核服务**（对外服务标题 `CHECK_PAGE API Server`）。

它以 **FastAPI** 暴露 HTTP 接口，以 **RocketMQ** 承接异步任务，围绕"文档 → 文本 → 结构化实体 → 合规判定"这条主线，提供：

1. **文档解析**：调用 MinerU 服务把 PDF/Word/WPS 等文件解析成 Markdown、middle_json（带坐标的中间结构）、分块文本。
2. **实体抽取**：基于 LLM Agent（Qwen 系列）从文本块中抽取结构化字段，支持规则预处理跳过 LLM。
3. **一致性 / 合规审查**：按审查规则判定文档内容是否合规，输出风险原文、修改建议及其在文档中的 bbox 定位。
4. **版式检测**：签到表签名检测、印章检测、盖章页签名检测、空白页检测、目录（Catalogue）存在性检测、PDF 页码连续性检测。
5. **合同专项核查**：合同金额 / 付款方式 / 分期付款一致性核查。
6. **向量检索**：基于 Milvus 的文档分块混合检索（Dense + BM25 双路 + RRF 融合）。
7. **文件处理**：文件上传、WPS/LibreOffice 格式互转、PDF 转图片、标题-图片提取、PDF 目录（TOC）提取。

典型业务场景为**招投标文件 / 合同 / 资质证照的自动合规审查**。

---

## 二、技术栈

### 2.1 运行时与基础

| 类别 | 技术 | 版本 / 说明 |
|---|---|---|
| 语言 | Python | 3.11（基础镜像 `python:3.11-slim-bullseye`） |
| Web 框架 | FastAPI | 0.135.1 |
| ASGI 服务器 | Uvicorn | 0.40.0，多 worker（`API_WORKERS=4`） |
| 数据校验 | Pydantic | 随 FastAPI 引入，`BaseResponse` / `ListResponse` / `DeleteRequest` |
| 跨域 | Starlette CORSMiddleware | `OPEN_CROSS_DOMAIN` 开关 |
| 配置 | YAML + 环境变量 | `config/base.py` + `config/models.yml` + `config/prompts.yml` |
| 日志 | logging + TimedRotatingFileHandler | 按天切分，保留 7 份，`logs/*.log` |

### 2.2 LLM / Agent

| 类别 | 技术 | 说明 |
|---|---|---|
| LLM 调用框架 | `langchain` 1.3.4 + `langchain-openai` 1.1.7 | `create_agent` + `ChatOpenAI`，统一 OpenAI 兼容协议 |
| 直连调用 | `openai` 2.41.1 / `httpx` 0.27.0 | Embedding 用 `AsyncOpenAI`；`invoke_llm` 用 `httpx` 直连 `/chat/completions` |
| Agent 框架（备用） | `agentscope` | `loader/agent_loader.py` 提供 `ReActAgent` 工厂，当前主链路未使用 |
| 模型服务 | vLLM / OpenAI 兼容端点 | 见 §6 模型清单 |
| 输出修复 | `json_repair` | 所有 LLM JSON 输出均经 `repair_json` + `loads` 容错解析 |
| 并发控制 | `asyncio.Semaphore`（按模型隔离） | `tools/llm/concurrency.py` |

### 2.3 存储与中间件

| 类别 | 技术 | 版本 / 说明 |
|---|---|---|
| 向量数据库 | Milvus（`pymilvus`） | 2.6.9，`172.25.67.60:19530`，DB `paragraph_summary` |
| 缓存 / 去重 / 版本控制 | Redis（`redis-py`） | 7.3.0，MD5 结果去重、task 版本号（停止信号）、章节提取缓存 |
| Java 序列化兼容 | `javaobj-py3` | 0.4.4，解析 Redis 中 Java 端写入的 version 对象 |
| 消息队列 | RocketMQ（`rocketmq-client-python`） | 2.0.0，底层依赖 `rocketmq-client-cpp` 2.0.0（deb 包本地安装） |
| 词面检索 | `rank_bm25` 0.2.2 + `jieba` 0.42.1 | BM25Okapi 两路召回（正文 / 标题） |

### 2.4 文档与图像处理

| 类别 | 技术 | 说明 |
|---|---|---|
| PDF 解析 | PyMuPDF（`pymupdf` 1.26.7） | 文本/图片/绘图提取、页面渲染、图片 bbox |
| 版式解析 | MinerU（外部 HTTP 服务） | `http://172.25.67.60:8002/file_parse` |
| 目标检测 | Paddle 版面检测服务（外部 HTTP） | `http://172.25.67.120:5555/api/layout/seal_recognition[Folder]` |
| 格式转换 | LibreOffice（系统级） | `soffice --headless`，WPS/Office → PDF 等 |
| 图片处理 | Pillow | 缩放、绘制检测框 |
| 基础计算 | numpy 1.26.4 | — |
| 其他 | python-docx、pypdf、nltk | 依赖中声明，主链路使用较少 |

### 2.5 部署

| 类别 | 说明 |
|---|---|
| 容器 | Docker + docker-compose（`agent-helper:test` 镜像） |
| 服务编排 | `main-service`（API，4 worker）+ `test-consumers`（消费者容器，按 glob 批量启动 `rocket_consumer_*.py` / `rocketmq_consumer_*.py`） |
| 资源限制 | main-service 16G 内存 / 4 CPU；consumers 8G / 2 CPU |
| 健康检查 | `curl http://localhost:8359/health`，间隔 300s，启动宽限 120s |
| 挂载卷 | `./config`、`./logs`、`/home/hgsc`（业务数据根目录） |
| 时区 | `Asia/Shanghai` |
| 中文字体 | Noto CJK / WQY 系列，支持 PDF 渲染与Office 转换 |

---

## 三、目录结构

```
agent-helper/
├── start.py                       # FastAPI 应用工厂 + 路由注册 + uvicorn 启动入口
├── main.py                        # 所有 HTTP 接口处理函数（22 个）
├── config/
│   ├── base.py                    # 全局配置常量（端口/路径/MQ/Redis/Milvus/并发）
│   ├── basic_config.py            # logging dictConfig、logger 实例
│   ├── models.yml                 # 模型连接配置（LLM / Embedding / Reranker）
│   ├── prompts.yml                # Prompt 模板（6 个）
│   └── prompts_backup.yml         # Prompt 备份
├── loader/
│   ├── base_loader.py             # load_entity_prompt / load_model_info / load_default_model_info
│   ├── prompt_loader.py           # PromptLoader（YAML 读取 + reload）
│   ├── model_loader.py            # ModelLoader（YAML 读取）
│   └── agent_loader.py            # agentscope ReActAgent 工厂（备用）
├── agent/                         # 五类业务 Agent 编排层
│   ├── entity_extract_agent.py    # 实体抽取（规则预处理 + 模型路由 + JSON 修复 + bbox 挂载）
│   ├── clean_entity_agent.py      # 实体清洗
│   ├── element_compliance_agent.py# 要素一致性审查 / 图片要素审查 / 目录版审查
│   ├── classfication_agent.py     # 标题分类（含幻觉重试 + 模糊匹配兜底）
│   └── summary_agent.py           # 文本摘要
├── tools/
│   ├── api_services/
│   │   ├── mineru_service.py      # MinerU 调用 + 结果落盘 + 去重 + 向量入库（核心）
│   │   └── entity_extract.py      # 资质有效期校验、文档有效性校验
│   ├── llm/
│   │   ├── invoke_agent.py        # langchain agent 调用（同步/异步）
│   │   ├── invoke_llm.py          # httpx 直连 chat/completions
│   │   ├── model_router.py        # 难度评估 → 模型路由
│   │   ├── concurrency.py         # 按模型隔离的 Semaphore
│   │   └── parse_response.py      # tool_call 剥离 / JSON / True-False 提取
│   ├── vector_db/
│   │   ├── milvus.py              # 入库 / 删除 / 混合检索（Dense+BM25+RRF）
│   │   ├── embedding.py           # Qwen3-Embedding 向量化
│   │   ├── rerank.py              # Qwen3-Reranker（默认关闭）
│   │   ├── kb_search.py           # 外部知识库 HTTP 检索
│   │   ├── eval_retrieval.py      # 检索评测脚本
│   │   ├── eval_title_retrieval.py# 标题检索评测脚本
│   │   └── measure_storage.py     # 存储量测量
│   ├── pdf_tools/
│   │   ├── mineru_parser.py       # middle_json 解析 + 行合并分块
│   │   ├── format_blocks → text_processing
│   │   ├── title_image_extractor.py # 标题-图片 JSON 提取（批量/单文件）
│   │   ├── image_extractor.py     # PDF 图片 bbox 提取（三策略）
│   │   ├── page_checker.py        # PDF 页码连续性检测
│   │   ├── toc_extractor.py       # PDF 目录（书签）提取
│   │   └── pdf_doc_cache.py       # fitz.Document LRU 缓存池
│   ├── detection/
│   │   ├── seal_sign_detector.py  # 签名/印章/空白页/目录/MinerU 章节提取（核心）
│   │   ├── seal_compliance.py     # 印章加盖合规性判定
│   │   └── coordinate_utils.py    # 坐标归一化 / 距离 / 邻近判断
│   ├── compliance/
│   │   ├── rule_patterns.py       # 正则规则库（40+ 模式）+ 校验器
│   │   ├── contract_amount.py     # 合同金额 / 付款方式核查
│   │   ├── index_table.py         # 索引表（目录）一致性审查
│   │   └── review_agent.py        # 审查 Agent（备用实现）
│   ├── text_processing/
│   │   ├── chinese_splitter.py    # 中文句子级切分
│   │   ├── hierarchical_splitter.py # 层级（L1/L2/L3）分块 + 表格保护
│   │   ├── format_blocks.py       # 分块落盘 + TOC 深度分块
│   │   └── remove_image_text.py   # 按图片 bbox 清除图片内文字
│   ├── file_io/
│   │   ├── upload.py              # 文件上传落盘
│   │   ├── convert_wps.py         # LibreOffice 格式转换
│   │   ├── convert_path.py        # Windows 路径规范化
│   │   └── pdf_to_image.py        # PDF → 图片（整本/单页/缩放）
│   ├── utils/
│   │   ├── base.py                # BaseResponse / ListResponse
│   │   ├── logger.py              # 消费者日志工厂
│   │   ├── redis_util.py          # Redis 封装（版本/去重/实体齐全/章节缓存）
│   │   ├── mineru_dedup.py        # MD5 去重 + 结果复制 + 注册
│   │   └── draw_boxes.py          # 检测框可视化
│   └── _legacy/                   # 30 个历史脚本归档（与主模块一一对应，不参与运行）
├── 8 个根级 RocketMQ 脚本           # 1 个 Producer 调试脚本 + 7 个消费者
├── docker-compose.yml / Dockerfile / requirements-full.txt / environment.yml
└── logs/ temp/
```

---

## 四、系统架构与数据流

### 4.1 整体架构

```
                 ┌──────────────────────────────────────────────┐
  业务系统 ──►   │  RocketMQ（7 类业务 topic，各自 produce/consume）│
                 └──────────────┬───────────────────────────────┘
                                │ PushConsumer（线程池 20，future timeout 6000s）
                                ▼
                    ┌────────────────────────┐
                    │  7 个消费者进程          │  ── 版本号校验（Redis task:{id}:version）
                    └───────────┬────────────┘
                                │ httpx POST（timeout 600~3600s）
                                ▼
   ┌────────────────────────────────────────────────────────────────────┐
   │  FastAPI 服务（start.py 注册 22 个路由，uvicorn 4 workers）           │
   │  ┌────────────┬────────────┬───────────┬──────────┬─────────────┐  │
   │  │ Agent 层    │ 检测层      │ 合规层     │ PDF/IO层 │ 向量库层     │  │
   │  └──────┬─────┴──────┬─────┴─────┬─────┴────┬────┴──────┬──────┘  │
   └─────────┼────────────┼───────────┼──────────┼───────────┼─────────┘
             ▼            ▼           ▼          ▼           ▼
        LLM 服务     Paddle 版面  规则引擎   MinerU 服务   Milvus
     Qwen3.6-27B    检测服务              LibreOffice    Redis
     -INT4 / 27B    (:5555)              (:8002)
     Embedding 0.6B
     Reranker 0.6B
```

### 4.2 主链路：文档解析（text_extract）

```
file2text_dir(file_list)
  ├─ 1. find_existing_result_dir(path) → file_md5 → Redis `mineru:result:{md5}`
  │     命中 → copy_result_files() 复制 9 类结果文件 → 跳过 MinerU（复制失败回退）
  ├─ 2. 线程池提交 upload_file（受 _mineru_sem = MINERU_MAX_CONCURRENCY=4 限制）
  │     POST MinerU /file_parse  {backend: vlm-auto-engine, return_middle_json: true}
  ├─ 3. 结果落盘 9 类文件：
  │     {name}.txt / _middle_original.json / _middle.json
  │     _device.txt / _device_original.txt / _texts.json / _toc.json ...
  ├─ 4. 图片文字清洗（三策略融合 → image_map → remove_image_text）
  │     ① PDF：PyMuPDF get_pdf_images_with_bbox
  │     ② 非 PDF：先 LibreOffice 转 PDF 再检测
  │     ③ middle_json 中 type=="image" 的 para_block bbox 兜底
  ├─ 5. 双轨分块：merge_lines_by_limit(max_len=MAX_LENGTH=512)
  │     对 raw middle_json 与 cleaned middle_json 各执行一次
  ├─ 6. TOC 二次深度分块（条件：pdf 存在 且 page_count > 5）
  │     process_and_save_with_toc() → 滑动窗口 4000 字 / overlap 200 字
  ├─ 7. 向量入库（条件：PDF 且 page_count > 5）
  │     put_into_vector_database(texts_path, "doc_" + file_id)
  └─ 8. register_mineru_result(md5, save_path)（SETNX 首个写入者获胜）
```

### 4.3 主链路：实体抽取

```
run_concurrent_entity_agents(file_list)
  对每个 file:
    ├─ entity_types 为空 → 直接返回 {}（不调 LLM）
    ├─ preprocess=True → _try_rule_extract()（正则规则）
    │     全部字段命中 → 跳过 LLM（统计规则命中率）
    │     部分命中     → 记录缺失字段，回退 LLM 补全
    └─ assess_entity_extract_difficulty(text, entity_types) → 模型路由
         └─ async_get_agent_response(prompt, input_text, model, temperature=0.2)
              └─ get_llm_semaphore(model) 限流 → asyncio.to_thread → langchain agent
   结果后处理：
    _parse_llm_result → 去 <think> → _dedup_json_keys（折叠重复 key）
      → repair_json → _pick_dict_from_list → clean_output（电话纯数字、日期区间归一化）
    traverse_result → 递归为叶子节点挂 bbox/page_idx/page_size
    merge_same_bbox_fields → 同 bbox 的列表项合并为逗号连接串
   并发：asyncio.Semaphore(ENTITY_EXTRACT_MAX_CONCURRENT=3)
```

### 4.4 主链路：合规审查

```
run_compliance_audit_agents(review_list)
  对每个 review_element（object / rule / knowledge_base_ids / preprocess）:
    ├─ preprocess=True → try_rule_based_check(rule, text) → 命中则跳过 LLM
    ├─ knowledge_base_ids → query_kbs_concurrently()（并发 10，外部知识库 HTTP）
    ├─ 拼装 input_text：规则 + 规则参考资料 + 审查对象 + 7 条审查要求
    └─ assess_compliance_difficulty() → 模型路由 → asyncio.gather 并发调用
   后处理：
    repair_json → 强制 result ∈ {"true","false"}（缺失一律置 "true"）
    find_risk_content_location(items, risk) → 为 risk_content 匹配 bbox/page_idx/page_size
```

---

## 五、功能模块清单

### 5.1 Agent 层（`agent/`）

| 模块 | 入口函数 | 功能 |
|---|---|---|
| `entity_extract_agent.py` | `run_concurrent_entity_agents(file_list)` | 批量实体抽取。规则预处理 + 模型路由 + 并发（3）+ JSON 修复（去 think / 折叠重复 key / 取最完整 dict）+ 输出清洗（电话纯数字、日期区间 `YYYY.MM.DD-YYYY.MM.DD`）+ bbox 挂载与合并 |
| `clean_entity_agent.py` | `run_clean_entity_agents(entity_list)` | 按 `requirement` 对 `object` 做格式清洗，输出 `{"cleaned_object", "key", "object"}` |
| `element_compliance_agent.py` | `run_compliance_audit_agents(review_list)` | 要素一致性核查（支持知识库参考资料、风险定位） |
| | `run_compliance_audit_agents_dir(review_list)` | 输入为**文件路径**版本，逐行读取 device.txt 做审查 |
| | `run_image_element_audit_agents(review_list)` | 图片要素核查（判断式陈述真假，规则预处理 + 当前日期注入 prompt） |
| `classfication_agent.py` | `run_classification_agents(query, class_list)` | 标题分类。**三重校验**：精确匹配 → `difflib` 模糊匹配（cutoff=0.6）→ 判定幻觉重试（最多 3 次，带负反馈提示） |
| `summary_agent.py` | `run_summary_agents(query)` | 文本摘要，输出 `{"summary": "..."}` |

### 5.2 LLM 层（`tools/llm/`）

| 模块 | 关键能力 |
|---|---|
| `invoke_agent.py` | `get_agent_response` / `async_get_agent_response`；`langchain.create_agent` + `ChatOpenAI`；`extra_body={"chat_template_kwargs": {"enable_thinking": False}}` 关闭思考模式；timeout 300s |
| `invoke_llm.py` | `httpx` 直连 `/chat/completions`；`max_tokens=2048`、`temperature=0.7`；timeout 300s |
| `model_router.py` | 难度评估：文本长度 > 6000 字 / 实体类型数 > 15 / 含复杂字段关键词（大于、小于、不低于、不超过、至少、至多）**至少满足 2 项**才升级 `Qwen3.6-27B`，否则 `Qwen3.6-27B-INT4` |
| `concurrency.py` | 每模型独立 Semaphore，上限 = `models.yml.llm_max_workers // API_WORKERS`。未定义模型回退默认模型 |
| `parse_response.py` | 剥离 `<tool_call>`、尝试 JSON、提取 `True`/`False` |

### 5.3 文档解析层（`tools/api_services/`、`tools/pdf_tools/`、`tools/text_processing/`）

| 模块 | 关键能力 |
|---|---|
| `mineru_service.py` | 核心编排（见 §4.2）。`file2text`（UploadFile/bytes/路径）、`file2text_dir`（文件列表）、`upload_file_bytes`、`extract_direct_blocks`（非 PDF/Word 单块合并） |
| `mineru_parser.py` | `extract_pdf_toc`；`EnhancedHierarchicalTextSplitter`；`merge_lines_by_limit(pdf_info, max_len=512)` 行合并分块 |
| `title_image_extractor.py` | `extract_title_image_json(pdf_path, save_path, env)` 提取标题与对应图片；`extract_title_image_batch` 批量版；`find_best_y0` 按 y 坐标定位标题 |
| `image_extractor.py` | `convert_to_pdf`（非 PDF 转 PDF）；`get_pdf_images_with_bbox`（header_ratio=0.1 排除页眉）；`extract_image_bboxes_from_middle_json` |
| `page_checker.py` | `check_pdf_page_continuity` 页码连续性：提取页码 → 支持罗马数字 → 分析是否连续/重复/缺页 |
| `toc_extractor.py` | `extract_pdf_toc` 从 PDF 书签提取目录 |
| `pdf_doc_cache.py` | `fitz.Document` LRU 缓存池（`get_document` / `return_document`），避免重复打开大 PDF |
| `hierarchical_splitter.py` | 层级分块：识别 L1/L2/L3 标题 → 按阈值切分 → **表格区间保护**（`_extract_table_ranges`）→ 安全切分点选择 → 强制合并过小块。默认 `chunk_size=8000` |
| `chinese_splitter.py` | `ChineseTextSplitter(pdf, sentence_size=512)`，中文标点断句 |
| `format_blocks.py` | `save_formatted_blocks_and_texts`（分块落盘，策略 `simple`）；`process_and_save_with_toc`（TOC 深度分块，滑动窗口 4000 字 / overlap 200 字，递归按标题层级切分） |
| `remove_image_text.py` | 按图片 bbox 清除图片区域内的文本 span（重叠率 / 包含关系双判定） |
| `entity_extract.py` | `date_compare`（日期是否仍在有效期）、`_process_partial_date`（不完整日期补全）、`extract_text_with_mineru`、`validate_document_validity`、`extract_and_validate_document_validity`（资质文件有效期校验，重试 `max_retries=1`） |

### 5.4 检测层（`tools/detection/`）

| 函数 | 功能 | 实现要点 |
|---|---|---|
| `mineru_get_middle_json(pdf_path)` | 获取/复用 MinerU middle_json | Redis 章节缓存 `mineru:section:{md5}:{keyword}`，TTL 3600s |
| `extract_section_from_mineru(pdf_path, title_keyword="成果产出指标")` | 按标题关键字抽取章节内容 | 解析表格 HTML（`TableHTMLParser`），统计行数，返回结构化 section |
| `mineru_extract_sign(pdf_path)` | 从 middle_json 定位签到表 | 匹配表格块，`_count_table_rows` 计算应有签名数 |
| `detect_sign(pdf_path)` | **签到表签名检测** | 定位签到表页 → 渲染为 PNG（dpi=200）→ POST Paddle 版面检测 `LAYOUT_API_URL_SINGLE` → 统计 `sign` 标签数 → `签名数 >= 表格行数` 判定完整 |
| `detect_seal(pdf_path)` | **首页印章检测** | 首页渲染 → 版面检测 → 统计 `seal` 标签 |
| `detect_sign_with_seal(pdf_path)` | **盖章页是否同时有签名** | 定位盖章页 → 检测该页 `sign` 标签 |
| `find_blank_pages(pdf_path, header_ratio=0.10, footer_ratio=0.10)` | **空白页检测** | 排除页眉页脚后，主体区域无文字、无图片、无 >2pt 矢量绘图 → 判为空白页 |
| `detect_catalogue(pdf_path)` | **目录存在性检测** | 渲染前 N 页 → 版面检测 → 匹配目录特征 |
| `seal_compliance(body_seal, body_seal_doc, ...)` | **印章加盖合规性** | 提取印章/签名坐标 → 坐标归一化到同一尺度 → 邻近判定（`_is_nearby`，multiplier 可调）→ 无效模式过滤（`_matches_invalid_pattern`）→ 判定印章与签名/文字的相对位置是否合规 |
| `coordinate_utils.py` | 坐标工具 | 坐标提取、距离计算、长边长度、跨尺度归一化、坐标换算 |

> 所有 Paddle 版面检测 HTTP 调用均走 `_httpx_post_with_retry(url, json, timeout=600, max_retries=3, backoff_base=2)`，仅对 `httpx.ReadError` 与 5xx 做指数退避重试。

### 5.5 合规层（`tools/compliance/`）

| 模块 | 功能 |
|---|---|
| `rule_patterns.py` | **正则规则库与校验器**（见 §5.6 详表） |
| `contract_amount.py` | 合同金额核查：`verify_contract_amount(pdf_path, file_id)`。经 MinerU 解析 → `_parse_contract_amount` → 按标题/正文关键词定位金额块 → 抽取总金额（支持 HTML 表格、阿拉伯数字、人民币大写）→ `_extract_payment_info` 抽取付款方式 → `_extract_installment_details` 解析分期付款（表格/文本两种解析）→ 与向量库/明细比对一致性 |
| `index_table.py` | `check_index_table(middle_old_path)`：索引表（目录）与正文标题一致性审查 |
| `review_agent.py` | 备用审查 Agent 实现（`create_check_agent` + `get_result`，带 `Model` Pydantic 结构化输出） |

### 5.6 规则库（`tools/compliance/rule_patterns.py`）

**三类校验级别**：
- `format`：仅校验格式匹配
- `value`：校验值的合法性（如校验位）
- `validity`：有效期校验（提取日期与当前日期比对）

**规则类型映射（`RULE_KEYWORD_MAP`，按序优先匹配）**：

| 关键词 | 规则类型 | 级别 |
|---|---|---|
| 手机号/手机号码/联系电话/联系方式/电话号码/电话 | `phone` | format |
| 邮箱/电子邮箱/电子邮件/E-mail/email/邮件 | `email` | format |
| 统一社会信用代码/信用代码/社会信用代码 | `credit_code` | value |
| 资质/资质文件/资质证书/许可证/营业执照 | `validity_period` | validity |
| 身份证有效期/证件有效期/有效期/有效日期/截止日期/到期日/起止日期 | `validity_period` | validity |
| 身份证/身份证号/身份证号码/公民身份号码 | `id_card` | value |
| 日期/日期格式 | `date` | format |
| 大写金额/金额大写/人民币大写 | `amount_cn` | format |
| 金额/合同金额/总金额/价款/费用/报价/投标报价 | `amount` | format |
| 网址/链接/URL/网站/官网 | `url` | format |
| 邮编/邮政编码 | `zip_code` | format |
| 固定电话/座机/固话/办公电话 | `landline` | format |
| IP地址/IP/服务器地址 | `ip` | format |
| 银行卡/银行卡号/银行账号/开户行账号 | `bank_card` | format |

**正则模式库（`PATTERNS`，40+ 条）**：`phone`、`email`、`credit_code`、`id_card`、`date`、`amount`、`amount_cn`、`url`、`zip_code`、`landline`、`ip`、`bank_card`、`validity_period`、`validity_end_date`、`issue_date`、`validity_duration`、`certified_entity`、`certification_body`、`cert_name`、`cert_title`，以及通用实体系列 `entity_name`、`entity_phone`、`entity_email`、`entity_degree`、`entity_school`、`entity_major`、`entity_graduation`、`entity_gender`、`entity_age`、`entity_birth`、`entity_credit_code`、`entity_id_card`、`entity_project_name`、`entity_subject_name`、`entity_sub_subject_name`、`entity_leader`、`entity_undertaker`、`entity_organizer`、`entity_compiler`、`entity_project_type`、`entity_period`。

**校验函数**：
- `_validate_credit_code`：统一社会信用代码 GB32100 校验位算法（权重 `CREDIT_CODE_WEIGHTS` + 字符集 `CREDIT_CODE_CHARS`）
- `_validate_id_card`：身份证 mod-11-2 校验位
- `_validate_date` / `_validate_email` / `_validate_phone`
- `_cn_num_to_int`：中文数字（含"两"）转阿拉伯数字
- `_extract_validity_dates`：有效期起止日期提取（支持"有效期 3 年"推算）
- `_check_validity`：返回 `(是否有效, 结束日期字符串, 说明)`
- `_extract_credential_info`：资质文件主体（公司名 / 证书名）提取
- `match_rule_type` / `check_format` / `try_rule_based_check`：规则匹配主入口
- `_try_text_match_rule` / `_build_text_match_result`：文本匹配型规则（等于 / 包含 / 字段包含三种模式）
- `find_entity_rule(entity_key)`：实体键 → `(正则, 清洗函数)` 映射，供实体抽取规则预处理使用

### 5.7 向量库层（`tools/vector_db/`）

| 模块 | 功能 |
|---|---|
| `milvus.py` | **写入**：`put_into_vector_database(json_dir, uuid)`，逐条过滤（跳过"财务报告"章节与空文本）、按字节截断（text 65000B / title 255B / 其余 127B）、批量 embedding、`insert` + `flush` + `load`。幂等：collection 已存在则跳过。<br>**删除**：`delete_from_vector_database(uuid, expr)`，支持整 collection `drop()` 或按布尔表达式 `delete`。<br>**检索**：`search_from_vector_database(query, uuid, top_k)` 四路融合（见下）。 |
| `embedding.py` | `text_embedding(text_input, is_query)`；`AsyncOpenAI`；单条最大 6000 字符（`_safe_truncate`，超长逐级缩短重试）；可选 `query_instruction` 前缀 |
| `rerank.py` | `rerank_documents(query, documents, top_n)`；调用 `/v1/rerank`（Qwen3-Reranker-0.6B）。**默认关闭**（`RERANK_ENABLED=False`） |
| `kb_search.py` | 外部知识库 HTTP 检索；`KB_URL=http://172.25.67.120:5050`；`top_k=1`、`similarity_threshold=0.5`、`metric_type=COSINE`；Bearer Token 鉴权；`query_kbs_concurrently` 信号量并发 10，timeout 30s |
| `eval_retrieval.py` / `eval_title_retrieval.py` | 检索质量评测：自动构造 query、多策略召回对比、`metrics()` 计算 MRR/Recall@k（k=3,5,10） |
| `measure_storage.py` | Milvus 存储量测量 |

**Milvus Collection Schema**（collection 名 = `doc_{file_id}`）：

| 字段 | 类型 | 参数 |
|---|---|---|
| `id` | INT64 | 主键，`auto_id=True` |
| `embedding` | FLOAT_VECTOR | `dim=1024` |
| `text` | VARCHAR | `max_length=65535` |
| `title_parent` | VARCHAR | 256 |
| `title` | VARCHAR | 256 |
| `title_class` | VARCHAR | 128 |
| `page_idx` | VARCHAR | 128 |

索引：`HNSW`，`metric_type=IP`，`params={"M":16, "efConstruction":200}`，检索 `ef=128`。

**混合检索算法**：
1. Dense 召回：`top_k * 5`（下限 50）条候选
2. 全局词面语料：`query(expr="id >= 0", limit=5000)`；失败则回退用 dense 池自身
3. BM25 两路：`jieba` 分词 → `BM25Okapi`，一路对正文 `text`，一路对标题 `title_parent + title`
4. 三路 RRF 融合：`score += 1/(RRF_K=60 + rank)`；不在 dense 池中的块按下限 `N` 惩罚
5. （可选）第四路 Reranker：对 RRF Top-30 重排，默认关闭

> 注释说明：关闭 Reranker 的原因是评测显示其持续拉低召回质量——标题检索 MRR 0.51 vs 三路 RRF 0.84；正文问题 MRR 0.50 vs 纯向量 0.75。

### 5.8 文件 IO 与工具层（`tools/file_io/`、`tools/utils/`）

| 模块 | 功能 |
|---|---|
| `upload.py` | `run_upload_file(file, save_path)` 上传文件落盘 |
| `convert_wps.py` | `convert_wps_libreoffice(input_path, output_dir, target_format="pdf")` 任意格式互转 |
| `convert_path.py` | `normalize_relative_path(path)` Windows `\` → `/` 规范化 |
| `pdf_to_image.py` | `pdf2img`（整本，dpi=250）、`pdf2img_single`、`resize_image`（默认 256×256）、`pdf_page_to_image(pdf_path, page_numbers, output_dir, dpi=200)` |
| `base.py` | `BaseResponse(code, msg, data)` / `ListResponse`；code 约定 200 成功、400 参数错误、500 服务异常 |
| `logger.py` | `setup_consumer_logger(consumer_name)` → `logs/<name>/<name>.log`，按天切分保留 7 份 |
| `redis_util.py` | `RedisUtil(env)`：`get_version` / `incr_version` / `set_version`（兼容 Java 序列化 `\xac\xed` 对象）、`get|set_mineru_result_dir`、`set|get_entity_collected`、`set|get_entity_target`、`set_entity_complete|is_entity_complete`、`get|set_section_cache`（TTL 3600s） |
| `mineru_dedup.py` | `file_md5`（1MB 分块）、`find_existing_result_dir`、`copy_result_files`（9 类结果文件）、`register_mineru_result`（SETNX）、`_dir_has_result` |
| `draw_boxes.py` | `draw_boxes(image_path, annotations, output_path)` 检测框可视化 |

---

## 六、模型清单（`config/models.yml`）

| 用途 | 模型 | 端点 | 并发上限 `llm_max_workers` |
|---|---|---|---|
| 默认 / 普通 LLM | `Qwen3.6-27B-INT4` | `http://172.25.67.120:9997/v1` | 40 |
| 复杂任务 LLM | `Qwen3.6-27B` | `http://172.25.67.140:9997/v1` | 20 |
| Embedding | `Qwen3-Embedding-0.6B`（1024 维） | `http://172.25.67.60:8001/v1` | — |
| Reranker | `Qwen3-Reranker-0.6B` | `http://172.25.67.60:8003/v1` | —（默认关闭） |

**并发说明**：`llm_max_workers` 是**全局**并发上限，uvicorn 每个 worker 持有独立 Semaphore，故单进程上限 = `llm_max_workers // API_WORKERS`。实际总并发 = `API_WORKERS(4) × 单进程上限` = `llm_max_workers`。

**Prompt 模板（`config/prompts.yml`）**：

| Key | 用途 |
|---|---|
| `extract_entities_prompt` | 实体抽取。含 11 条输出要求：时间格式 `YYYY.MM.DD`（保留原文精度）、键唯一性、禁止编造、多实体防丢失、**防止跨主体误抽**（区分"我方投标人"与"他方客户/案例/简历/母公司"） |
| `entity_clean_prompt` | 实体格式清洗 |
| `compliance_audit_prompt` | 合规审查专家，输出 `[{rule, result, risk_content, advise}]`；未命中关键词视为合规通过 → `"true"` |
| `image_element_audit_prompt` | 图片要素审核。含日期/数值/资质/合同/陈述五类范围；模板内置占位日期 `当前日期是2026-01-22`，运行时替换为真实当天日期 |
| `summary_prompt` | 文本摘要，输出 `{"summary": "..."}` |
| `classification_prompt` | 标题分类，必须原样返回候选列表中的标题 |

---

## 七、HTTP API 清单

服务端口 `8359`，Swagger 文档 `/docs`（根路径 `/` 重定向至 `/docs`），健康检查 `GET /health`。
统一响应体：`BaseResponse(code, msg, data)`。

| # | 方法 | 路径 | Tag | 说明 | 请求参数 |
|---|---|---|---|---|---|
| 0 | GET | `/health` | — | 健康检查，返回 `{"status":"healthy"}` | 无 |
| 1 | POST | `/api/mineru/textExtract` | MinerU | 文本抽取（文件上传） | `file_list: list[UploadFile]`（multipart） |
| 2 | POST | `/api/mineru/textExtractDir` | MinerU | 文本抽取（服务器路径） | `{"file_list": [{"file_path": ...}]}`（embed） |
| 3 | POST | `/api/title-image/extract` | TitleImage | PDF 标题-图片提取 | `pdf_path`, `save_path`, `env` |
| 4 | POST | `/api/agent/extractEntity` | Agent | 实体抽取 | `{"file_list": [{file_name, text, entity_types, items, preprocess}]}` |
| 5 | POST | `/api/agent/extractEntityClean` | Agent | 实体抽取结果清洗 | `{"entity_list": [{key, object, requirement}]}` |
| 6 | POST | `/api/agent/complianceAudit` | Agent | 要素一致性核查 | `{"review_list": [{object:{text,items}, rule, knowledge_base_ids, preprocess}]}` |
| 7 | POST | `/api/agent/complianceAuditDir` | Agent | 要素一致性核查（原文路径版） | `{"review_list": [{object: 文件路径, rule}]}` |
| 8 | POST | `/api/agent/imageElementAudit` | Agent | 图片要素一致性核查 | `{"review_list": [{object, preprocess}]}` |
| 9 | POST | `/api/agent/classification` | Agent | 文本分类（标题匹配） | `query: str`, `class_list: list` |
| 10 | POST | `/api/agent/summary` | Agent | 文本摘要 | `query: str` |
| 11 | POST | `/api/milvus/search` | Milvus | 向量混合检索 | `rule`, `file_uuid`, `top_k` |
| 12 | POST | `/api/milvus/delete` | Milvus | 向量删除（单/批量） | `{"uuid": str \| list[str], "expr": str}` |
| 13 | POST | `/api/file/upload` | File | 文件上传到服务器 | `file: UploadFile`, `save_path: str` |
| 14 | POST | `/api/file/convert` | File | WPS/Office 格式转换 | `input_path`, `output_dir`, `target_format="pdf"` |
| 15 | POST | `/api/pdf/checkContinue` | PDF | PDF 页码连续性检查 | `pdf_path`, `env` |
| 16 | POST | `/api/detection/detectSign` | Detection | 签到表检测 | `pdf_path` |
| 17 | POST | `/api/detection/detectSeal` | Detection | 印章检测 | `pdf_path` |
| 18 | POST | `/api/detection/extractSection` | Detection | MinerU 章节内容提取 | `pdf_path`, `title_keyword="成果产出指标"` |
| 19 | POST | `/api/detection/findBlankPages` | Detection | 空白页检测 | `pdf_path` |
| 20 | POST | `/api/detection/catalogue` | Detection | 目录存在检测 | `pdf_path` |
| 21 | POST | `/api/Contract/checkSignaturewithSeal` | Contract | 盖章页签名检测 | `pdf_path` |
| 22 | POST | `/api/Contract/verifyContractAmount` | Contract | 合同金额核查 | `pdf_path`, `file_id` |

**关键实现说明**：
- 接口 18 `extractSection` 内部含阻塞 MinerU 调用与重解析，用 `asyncio.to_thread` 投递到线程池，避免阻塞事件循环。
- 接口 12 `delete` 支持批量：逐个删除并汇总成功数，单项失败不中断整体。
- 所有接口统一 `try/except` → `logger.error` → 返回 `code=500`；耗时接口记录 `elapsed_time`。

---

## 八、RocketMQ 消息接口清单

8 个根级脚本：1 个 Producer 调试脚本 + 7 个消费者。

### 8.1 Topic 与 Group 对照表

| 消费者脚本 | 消费 Topic | 消费 Group | 反馈 Topic | 反馈 Group | 调用接口 |
|---|---|---|---|---|---|
| `rocketmq_consumer_text_extract.py` | `mineru_text_extraction-program` | `mineru_consume_group` | `mineru_text_extraction-program-feedback` | `mineru_produce_group` | `MINERU_API_URL`（timeout 3600s） |
| `rocketmq_consumer_entity_extract.py` | `llm_entity_extrct-program` | `entity_extract_consume_group` | `llm_entity_extrct-program-feedback` | `entity_extract_produce_group` | `ENTITY_EXTRACT_API_URL` |
| `rocketmq_consumer_element_compliance.py` | `llm_element_compliance-program` | `element_compiance_consume_group` | `llm_element_compliance-program-feedback` | `element_compiance_produce_group` | `ELEMENT_COMPIANCE_API_URL` |
| `rocket_consumer_image_entity_extract.py` | `llm_image_entity_extract-program` | `llm_image_entity_extract_consume_group` | `llm_image_entity_extract-program-feedback` | `llm_image_entity_extract_produce_group` | — |
| `rocket_consumer_image_element_audit.py` | `llm_image_element_compliance-program` | `llm_image_element_compiance_consume_group` | `llm_image_element_compliance-program-feedback` | `llm_image_element_compiance_produce_group` | `RULER_COMPIANCE_API_URL`（timeout 600s） |
| `rocket_consumer_seal_compliance.py` | `llm_image_seal_compliance-program` | `llm_image_seal_compliance_consume_group` | `llm_image_seal_compliance-program-feedback` | `llm_image_seal_compliance_produce_group` | `LAYOUT_API_URL` / `LAYOUT_API_URL_SINGLE` / `ENTITY_EXTRACT_API_URL` / `MINERU_API_URL` |
| `rocket_consumer_otheruler_compliance.py` | `llm_other_rule_compliance-program` | `other-rule-consumer-group` | `llm_other_rule_compliance-program-feedback` | `other-rule-produce-group` | `MINERU_API_URL`（timeout 3600s） |

公共：日志 Topic `user_log-program-feedback` / Group `user_log_producer_group`（7 个消费者共用）。
Nameserver：test `172.25.67.60:9876`；formal `172.25.67.237:9876`。

`rocket_mq_send.py` 是**纯 Producer 调试脚本**（非消费者），提供 `send_seal_compliance` / `send_image_element` / `send_image_entity` / `send_mineru` / `send_entity_extract` / `send_element_compliance` 六个函数，用 `TEST_GROUP_ID_PROGRAM` 手工投递测试消息。

### 8.2 消费者统一机制

**并发模型**：
```
PushConsumer(topic) → consumer.set_thread_count(CONSUMER_MAX_WORKERS=20)
callback = lambda msg: threaded_callback(msg)
threaded_callback:
    future = ThreadPoolExecutor(20).submit(callback, msg)
    return future.result(timeout=6000)   # 100 分钟
    异常处理 → 记日志 → return ConsumeStatus.CONSUME_SUCCESS
```
> 所有分支最终都返回 `CONSUME_SUCCESS`，**从不返回 `RECONSUME_LATER`**，`MAX_RETRY_TIMES` 重试代码已全部注释 → 消息级无重试，失败仅通过 feedback topic 回传 `code=500`。

**Nameserver 看门狗**：
```
while True:
    wait_for_nameserver(host, port)    # socket 探测 timeout=3s
    consumer.start()
    while True:
        time.sleep(3600)               # 每 1 小时探测一次
        if not 连通: raise ConnectionError
except:
    consumer.shutdown(); time.sleep(10); 重建
```

**版本号校验（Redis 停止信号）**：
```
msg_version = int(version or 0)
redis_version = redis_util.get_version(task_id)   # key: task:{taskId}:version
if redis_version is None or redis_version > msg_version: → 跳过（CONSUME_SUCCESS）
```
处理完成后、发反馈前会做**第二次校验**，防止已停止任务回传脏数据。`get_version` 兼容 Java 序列化对象（`\xac\xed` 前缀走 `javaobj` 解析）。

**日志消息格式**（发往 `user_log-program-feedback`）：
```json
{"code": 200|500, "msg": "开始处理|处理中|处理完成|报错文案",
 "taskId": "...", "taskNo": "...", "text": "可读文案", "time": "YYYY-MM-DD HH:MM:SS"}
```

**路径处理**：`normalize_relative_path()` 将 Windows 风格相对路径转 `/`，再与 `ENV_CONFIG_MAP[env]["upload_root"]` 拼接。

---

## 九、外部依赖服务清单

| 服务 | 地址 | 用途 | 配置项 |
|---|---|---|---|
| MinerU 解析 | `http://172.25.67.60:8002/file_parse` | 文档版式解析（backend `vlm-auto-engine`） | `MINERU_URL` |
| Paddle 版面检测（批量） | `http://172.25.67.120:5555/api/layout/seal_recognitionFolder` | 印章/签名检测 | `LAYOUT_API_URL` |
| Paddle 版面检测（单文件） | `http://172.25.67.120:5555/api/layout/seal_recognition` | 印章/签名检测 | `LAYOUT_API_URL_SINGLE` |
| 默认 LLM | `http://172.25.67.120:9997/v1` | Qwen3.6-27B-INT4 | `models.yml` |
| 复杂 LLM | `http://172.25.67.140:9997/v1` | Qwen3.6-27B | `models.yml` |
| Embedding | `http://172.25.67.60:8001/v1` | Qwen3-Embedding-0.6B | `models.yml` |
| Reranker | `http://172.25.67.60:8003/v1` | Qwen3-Reranker-0.6B（默认关闭） | `models.yml` |
| 外部知识库 | `http://172.25.67.120:5050` | 合规规则参考资料检索 | `kb_search.py` 硬编码 |
| 结果文件上传 | `http://172.25.67.178:8891/common/uploadMineruResultFile` | 结果回传业务系统 | `UPLOAD_URL` |
| 备选 OCR | `http://172.25.67.120:8110` | 兜底 OCR | `QIANFAN_OCR_API_URL` |
| Milvus | `172.25.67.60:19530`，DB `paragraph_summary` | 向量存储/检索 | `MILVUS_*` |
| Redis | `172.25.67.232:6379`，test db0 / prod db1 | 缓存/去重/版本 | `REDIS_*` |
| RocketMQ | test `172.25.67.60:9876` / formal `172.25.67.237:9876` | 异步任务 | `ROCKETMQ_NAMESERVER*` |

---

## 十、关键配置项（`config/base.py`）

| 配置 | 默认值 | 说明 |
|---|---|---|
| `VERSION` / `PORT` | `0.0.1` / `8359` | 服务版本与端口 |
| `OPEN_CROSS_DOMAIN` | `False` | CORS 开关 |
| `CURRENT_ENV` | `test` | 环境选择（`test` / `formal`） |
| `UPLOAD_ROOT` / `UPLOAD_ROOT_FORMAL` | `/home/hgsc/file_test` / `/home/hgsc/file` | 文件根目录 |
| `API_WORKERS` | `4` | uvicorn worker 数（env `UVICORN_WORKERS`） |
| `CONSUMER_MAX_WORKERS` | `20` | 消费者线程池 + `set_thread_count` |
| `MINERU_MAX_CONCURRENCY` | `4` | MinerU 进程级并发闸门（全局 = `API_WORKERS × 4`） |
| `ENTITY_EXTRACT_MAX_CONCURRENT` | `3` | 实体提取任务并发（防 GPU 显存溢出） |
| `KB_QUERY_MAX_CONCURRENT` | `10` | 知识库检索并发 |
| `MAX_LENGTH` | `512` | 单文本块最大长度 |
| `ENTITY_MIN_TEXT_LEN` | `20` | 前置过滤：去空白后短于此值视为无实质内容 |
| `ENTITY_COMPLETE_CHECK_ENABLED` | `false` | 实体齐全检查开关（开启后目标实体已全提取则跳过后续 index） |
| `MAX_RETRY_TIMES` | `3` | **已 import 但未使用**（重试代码全被注释） |
| `EMBEDDING_DIMENSION` / `SEARCH_TOPK` | `1024` / `20` | Milvus 维度、默认召回数 |
| `RRF_K` / `VECTOR_CANDIDATE_MULTIPLIER` / `RERANK_CANDIDATE_NUM` | `60` / `5` / `30` | 混合检索参数 |
| `BM25_GLOBAL_LIMIT` | `5000` | BM25 全局语料上限（单文档最大分块数） |
| `RERANK_ENABLED` | `False` | Reranker 开关（评测显示有损，故关闭） |
| `REDIS_KEY_VERSION` | `task:{taskId}:version` | 版本号键（停止信号） |
| `REDIS_KEY_MINERU_RESULT` | `mineru:result:{md5}` | MinerU 结果去重索引 |
| `REDIS_KEY_SECTION_CACHE` | `mineru:section:{md5}:{keyword}` | 章节提取内容级缓存（TTL 3600s） |

---

## 十一、部署与运维

**构建**：`Dockerfile` 基于 `python:3.11-slim-bullseye`，安装 LibreOffice + 中文字体 + `rocketmq-client-cpp` deb 包，pip 走阿里云镜像。

**启动**：
```bash
docker-compose up -d            # 构建并启动
docker-compose up --build       # 强制重建
```
- `main-service`：`uvicorn start:app --host 0.0.0.0 --port 8359 --workers 4`
- `test-consumers`：`for consumer in rocket_consumer_*.py rocketmq_consumer_*.py; do python $consumer --env test & done && wait`

**日志**：
- 应用日志：`logs/agent_helper.log`（`TimedRotatingFileHandler`，midnight 切分，保留 7 天）
- 消费者日志：`logs/<consumer>_<env>/<consumer>_<env>.log`，同样按天保留 7 天

---

## 十二、已知问题与技术债

1. **`wait_for_nameserver` 硬编码**：`rocketmq_docker/time_restart.py` 内部忽略传入的 host/port，始终解析 `ROCKETMQ_NAMESERVER`（test 地址）。因此 `--env formal` 时消费者连的是 formal nameserver，但健康探测打的仍是 test 的 `172.25.67.60:9876`。

2. **消息级无重试**：所有消费者统一返回 `CONSUME_SUCCESS`，`MAX_RETRY_TIMES` 未生效，失败消息直接丢弃，仅通过 feedback topic 上报 `code=500`，需业务侧自行补偿。

3. **API Key 硬编码**：`config/models.yml` 与 `tools/vector_db/kb_search.py` 中的 Bearer Token 明文写入代码库，应迁移至环境变量或密钥管理。

4. **MinerU 调用无超时**：`requests.post(MINERU_URL, ..., timeout=None)`，长任务可能长期挂起占用并发额度。

5. **`milvus.py` 残留未用导入**：`from agent.summary_agent import run_summary_agents` 已导入但 summary 字段被注释未启用。

6. **评测脚本与生产索引参数不一致**：两个 eval 脚本建索引用 `HNSW M=8`，生产用 `M=16`，评测结论与实际线上表现存在偏差。

7. **拼写错误的历史遗留**：`config/base.py` 中 `REDIS_PORTT_PROD`、`REDIS_DBT_PROD`、`REDIS_PASSWORDT_PROD` 命名有误（已在代码注释中说明 `MAX_LENTH` → `MAX_LENGTH` 修正过一次）。

8. **`tools/_legacy/` 归档冗余**：30 个历史脚本与 `tools/` 下现役模块基本一一对应（如 `detection_seal_sign.py` ↔ `seal_sign_detector.py`、`verification_contract_amout.py` ↔ `contract_amount.py`），README 待办事项中"tools 内的小功能分类整理"尚未完成。

9. **`tools/detection/seal_compliance.py` 与 `tools/detection/coordinate_utils.py` 存在函数重复定义**（`_extract_seal_sign_coords`、`_matches_invalid_pattern`、`_is_nearby`、`_calculate_distance`、`_get_longer_side_length`、`_normalize_coordinates_to_same_scale`、`_convert_coordinates`），应抽取为单一工具模块。

10. **README 待办事项（未完成）**：
    - tools 内小功能分类整理
    - 实体一致性检查路由：简单功能走规则判断，普通走 Qwen3.6-27B-INT4，复杂走 Qwen3.6-27B
    - 实体提取：规则（主）+ LLM（兜底）
    - 压缩大模型输入内容长度

---

## 附录 A：核心依赖清单（`requirements-full.txt`）

```
fastapi==0.135.1        uvicorn==0.40.0         python-multipart==0.0.21
langchain==1.3.4        langchain-openai==1.1.7 openai==2.41.1
pymilvus==2.6.9         rank-bm25==0.2.2        jieba==0.42.1
rocketmq-client-python==2.0.0                   httpx==0.27.0
redis==7.3.0            javaobj-py3==0.4.4      pymupdf==1.26.7
Pillow                  numpy==1.26.4           python-docx
json_repair             nltk                    pypdf
```

## 附录 B：典型请求示例

```bash
# 1. 文本抽取（服务器路径）
curl -X POST http://localhost:8359/api/mineru/textExtractDir \
  -H "Content-Type: application/json" \
  -d '{"file_list": [{"file_path": "/home/hgsc/file/xxx.pdf"}]}'

# 2. 实体抽取
curl -X POST http://localhost:8359/api/agent/extractEntity \
  -H "Content-Type: application/json" \
  -d '{"file_list": [{
        "file_name": "resume.pdf",
        "text": "姓名：张三\n电话：13800138000\n...",
        "entity_types": {"姓名": "", "电话": "手机号码"},
        "preprocess": true
      }]}'

# 3. 合规审查
curl -X POST http://localhost:8359/api/agent/complianceAudit \
  -H "Content-Type: application/json" \
  -d '{"review_list": [{
        "object": {"text": "...", "items": [...]},
        "rule": "合同金额不低于100万元",
        "knowledge_base_ids": [],
        "preprocess": true
      }]}'

# 4. 印章检测
curl -X POST http://localhost:8359/api/detection/detectSeal \
  -H "Content-Type: application/json" -d '{"pdf_path": "..."}'

# 5. 向量删除（批量）
curl -X POST http://localhost:8359/api/milvus/delete \
  -H "Content-Type: application/json" \
  -d '{"uuid": ["doc_1", "doc_2"], "expr": null}'
```

# agent-helper 高级智能体升级方案

> 生成时间：2026-09-01
> 配套文档：`temp/技术报告.md`（现状分析报告）
> 目标：基于现有 `tools/` 工具集，将当前的「单轮 LLM 调用」升级为「自主规划、工具调用、多轮迭代」的高级智能体

---

## 一、现状诊断：为什么说现在的 Agent「简单」

### 1.1 现有 Agent 的实际形态

当前 5 个 Agent（`agent/` 目录）本质上都是**同构的单轮管道**，执行模式高度一致：

```python
# 以 entity_extract_agent.py 为例（其余 4 个结构相同）
async def run_concurrent_entity_agents(file_list):
    semaphore = asyncio.Semaphore(ENTITY_EXTRACT_MAX_CONCURRENT)  # 限流
    tasks = [_process_single_file(...) for file in file_list]
    await asyncio.gather(*tasks)                                   # 批量并发
    # 后处理：JSON 修复 → 输出清洗 → bbox 挂载
```

每个 Agent 的生命周期是：

```
拼 prompt → 调 LLM 一次 → 解析 JSON → 后处理 → 返回
```

关键特征：**LLM 只被调用一次，没有反馈循环，没有工具，没有记忆**。

### 1.2 五个具体局限

| # | 局限 | 表现 | 后果 |
|---|---|---|---|
| **L1** | **无工具调用** | LLM 只能"读文本、吐 JSON"，无法主动调用 `tools/` 下 40+ 个已实现的能力 | 明明有 `check_pdf_page_continuity`、`check_credit_code` 等确定性工具，却让 LLM 用自然语言猜 |
| **L2** | **无多轮迭代** | 单次调用定生死，没有"发现问题 → 补充取证 → 修正结论"的循环 | 遇到长文档、歧义规则时，一次抽不全就只能靠后处理硬凑 |
| **L3** | **无自主规划** | 流程由代码硬编码（`file2text → 分块 → 抽取`），LLM 不参与任务分解 | 换一类文档就要改代码，无法处理开放式任务（如"帮我审这份投标文件"） |
| **L4** | **无记忆/状态** | 每次调用独立，跨块、跨页、跨文件的上下文不共享 | 审查"前后一致性"（如目录与正文标题是否匹配）这类需求无法自动完成 |
| **L5** | **确定性计算依赖 LLM** | 格式校验、日期比较、金额一致性等本可用规则/代码解决的问题，交给 LLM 判断 | 成本高、延迟高、且 LLM 在精确计算上不可靠 |

### 1.3 造成的实际痛点

以**合规审查**为例，当前的 `run_compliance_audit_agents` 是：

```
规则 + 参考资料 + 审查对象文本 → 一次性丢给 LLM → 期望它输出 {result, risk_content, advise}
```

问题在于：
- **规则含"不低于 100 万"这类数值判断** → LLM 需要先找到金额、再比对、再下结论，三步合一，准确率随文本长度下降
- **风险原文需要 bbox 定位** → LLM 不知道坐标，只能靠后处理 `find_risk_content_location` 做字符串匹配，匹配失败就没有定位
- **多规则串行** → 每条规则独立调一次 LLM，规则间无法共享已提取的实体

### 1.4 一个被浪费的事实：框架能力已具备

`tools/llm/invoke_agent.py` 中：

```python
agent = create_agent(
    model=llm,
    # tools=tools,                    # ← 已支持，被注释
    system_prompt=prompt,
    # response_format=...,            # ← 已支持
    # checkpointer=checkpointer,       # ← 已支持（记忆）
    # store=store,                     # ← 已支持（长期存储）
    # middleware=[...],                # ← 已支持（人机协作、重试、 Fallback）
)
```

经实际验证，`langchain 1.3.4` 的 `create_agent` 签名完整包含：

```
model, tools, system_prompt, context_schema, response_format,
checkpointer, store, interrupt_before, interrupt_after, debug, middleware, name
```

**结论：升级的主要工作量不是引入新框架，而是把已有的 `tools/` 封装成 Agent 可调用的工具，并设计编排逻辑。**

---

## 二、目标架构：三层智能体

### 2.1 架构总览

```
┌─────────────────────────────────────────────────────────────────┐
│  L3 编排层  Orchestrator（规划 + 任务分解 + 结果聚合）             │
│  · 把"审这份投标文件"分解为若干子任务                              │
│  · 调度 L2 专家，管理共享状态（Blackboard）                        │
│  · 决定何时终止、何时转人工                                        │
└───────────────────────┬─────────────────────────────────────────┘
                        │ 派发子任务
        ┌───────────────┼───────────────┬───────────────┐
        ▼               ▼               ▼               ▼
┌──────────────┐ ┌────────────┐ ┌─────────────┐ ┌──────────────┐
│ L2 文档解析  │ │ L2 实体抽取│ │ L2 合规审查 │ │ L2 版式检测  │
│   Expert     │ │   Expert   │ │   Expert    │ │    Expert    │
│ · 格式判断   │ │ · 规则优先 │ │ · 取证链    │ │ · 多策略融合 │
│ · 解析策略   │ │ · LLM 兜底 │ │ · 交叉验证  │ │ · 置信度聚合 │
└──────┬───────┘ └─────┬──────┘ └──────┬──────┘ └──────┬───────┘
       └───────────────┴───────────────┴───────────────┘
                        │ 调用
                        ▼
┌─────────────────────────────────────────────────────────────────┐
│  L1 工具层  ToolSet（40+ 个原子工具，全部封装自现有 tools/）       │
│  ┌─────────┬─────────┬─────────┬─────────┬───────────────────┐  │
│  │ 解析类   │ 检索类   │ 校验类   │ 检测类   │ 计算/转换类        │  │
│  └─────────┴─────────┴─────────┴─────────┴───────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 与现有架构的关系：增量而非重写

| 现有资产 | 处置 | 说明 |
|---|---|---|
| 22 个 HTTP 接口 | **保留** | 继续对外服务，内部改为调用智能体 |
| 7 个 RocketMQ 消费者 | **保留** | 消息协议不变，消费者改为提交任务给 Orchestrator |
| `agent/` 下 5 个 Agent | **保留为原子能力** | 降级为 L2 专家的内部实现，对外不再直接暴露 |
| `tools/` 下 40+ 函数 | **封装为 Tool** | 加 `@tool` 装饰器 + Pydantic schema，函数体不动 |
| `config/prompts.yml` | **扩展** | 新增 Orchestrator / 各 Expert 的 system prompt |
| Milvus / Redis | **复用** | Milvus 作为 Agent 的长期记忆，Redis 作为短期状态 |

**核心原则：不动现有对外契约，只在内部把「固定管道」换成「智能体编排」。**

---

## 三、L1 工具层：把现有 tools 封装为 Agent 工具

### 3.1 封装方式

统一用 `langchain_core.tools.tool` 装饰器，配 Pydantic schema：

```python
# tools/agent_tools/parse_tools.py
from pydantic import BaseModel, Field
from langchain_core.tools import tool
from tools.pdf_tools.page_checker import check_pdf_page_continuity


class PageCheckInput(BaseModel):
    pdf_path: str = Field(description="PDF 文件的相对路径，如 upload/review/xxx/a.pdf")
    env: str = Field(default="test", description="环境：test / formal")


@tool("check_page_continuity", args_schema=PageCheckInput)
def check_page_continuity(pdf_path: str, env: str = "test") -> dict:
    """检查 PDF 页码是否连续，返回是否连续、断裂位置、缺失页码。

    适用场景：怀疑文档缺页、页码错乱、扫描件顺序错误。
    注意：这是确定性检查，结论可信，不需要再用 LLM 复核。
    """
    return check_pdf_page_continuity(pdf_path, env)
```

**三个设计规范**：

1. **description 要写清"何时用"和"结论可信度"** —— 直接决定 Agent 选对工具
2. **优先封装纯函数/幂等函数** —— 有副作用的操作（写库、发消息）需加人工确认
3. **返回值结构化** —— 返回 dict 而非字符串，便于 Agent 二次利用

### 3.2 工具清单（基于现有实现）

#### A. 解析类（来自 `tools/pdf_tools/`、`tools/api_services/`）

| 工具名 | 源函数 | 用途 | 幂等 |
|---|---|---|---|
| `parse_document` | `mineru_service.file2text` | 文档 → 文本/分块/middle_json | ✅（有 MD5 去重） |
| `extract_pdf_toc` | `toc_extractor.extract_pdf_toc` | 提取 PDF 书签目录 | ✅ |
| `extract_section` | `seal_sign_detector.extract_section_from_mineru` | 按标题抽取章节 | ✅（有缓存） |
| `extract_title_image` | `title_image_extractor.extract_title_image_json` | 标题-图片对应 | ✅ |
| `extract_image_bboxes` | `image_extractor.get_pdf_images_with_bbox` | 图片坐标 | ✅ |
| `pdf_to_image` | `pdf_to_image.pdf_page_to_image` | 指定页渲染为图片 | ✅ |
| `convert_format` | `convert_wps.convert_wps_libreoffice` | 格式转换 | ✅ |

#### B. 检索类（来自 `tools/vector_db/`）

| 工具名 | 源函数 | 用途 | 幂等 |
|---|---|---|---|
| `search_document` | `milvus.search_from_vector_database` | 语义+词面混合检索 | ✅ |
| `search_knowledge_base` | `kb_search.query_kbs_concurrently` | 外部规则知识库 | ✅ |
| `get_document_stats` | `measure_storage.*` | 文档规模/分块数 | ✅ |

> `search_document` 是**最关键**的工具：它让 Agent 不必把整篇文档塞进上下文，
> 而是"按需取证"——这正是解决长文档审查的核心手段。

#### C. 校验类（来自 `tools/compliance/rule_patterns.py`）—— **最高优先级**

| 工具名 | 源函数 | 用途 |
|---|---|---|
| `validate_credit_code` | `_validate_credit_code` | 统一社会信用代码 GB32100 校验位 |
| `validate_id_card` | `_validate_id_card` | 身份证 mod-11-2 校验位 |
| `check_validity_period` | `_check_validity` | 资质有效期是否仍有效 |
| `match_rule_type` | `match_rule_type` | 规则文本 → 规则类型 |
| `check_format_by_rule` | `check_format` | 按规则类型做格式校验 |
| `try_rule_check` | `try_rule_based_check` | 规则预处理总入口 |
| `extract_credential_info` | `_extract_credential_info` | 提取资质主体/证书名 |
| `cn_num_to_int` | `_cn_num_to_int` | 中文数字转阿拉伯数字 |
| `parse_date` | `_parse_date` | 日期解析（含不完整日期） |

> **这一类是投入产出比最高的**：它们全是确定性计算，当前却让 LLM 用自然语言判断。
> 封装后，Agent 会优先调用工具，只在规则未覆盖时才动用 LLM。

#### D. 检测类（来自 `tools/detection/`）

| 工具名 | 源函数 | 用途 |
|---|---|---|
| `detect_seal` | `seal_sign_detector.detect_seal` | 首页印章检测 |
| `detect_sign` | `seal_sign_detector.detect_sign` | 签到表签名检测 |
| `detect_seal_with_sign` | `seal_sign_detector.detect_sign_with_seal` | 盖章页签名 |
| `find_blank_pages` | `seal_sign_detector.find_blank_pages` | 空白页检测 |
| `detect_catalogue` | `seal_sign_detector.detect_catalogue` | 目录存在性 |
| `check_page_continuity` | `page_checker.check_pdf_page_continuity` | 页码连续性 |
| `check_seal_compliance` | `seal_compliance.seal_compliance` | 印章加盖合规性 |
| `check_index_table` | `index_table.check_index_table` | 目录与正文一致性 |
| `verify_contract_amount` | `contract_amount.verify_contract_amount` | 合同金额核查 |

#### E. 计算/转换类

| 工具名 | 源函数 | 用途 |
|---|---|---|
| `normalize_path` | `convert_path.normalize_relative_path` | 路径规范化 |
| `compare_coordinates` | `coordinate_utils.*` | 坐标距离/邻近判断 |
| `compute_similarity` | `difflib.SequenceMatcher` | 文本相似度（分类兜底） |

### 3.3 工具分层暴露策略

**不要把所有工具一次性塞给一个 Agent** —— 工具越多，选择准确率越低。按 Expert 分组：

```
Orchestrator（编排）  → 只给 4 个：dispatch_to_expert / read_blackboard / finish / ask_human
文档解析 Expert      → 7 个解析类工具
实体抽取 Expert      → 校验类(9) + 检索类(2)
合规审查 Expert      → 校验类(9) + 检索类(3) + 检测类(4)
版式检测 Expert      → 检测类(9) + 解析类(2)
```

经验值：单个 Agent 绑定 **5~10 个**工具时选择准确率最佳。

---

## 四、L2 专家层：从「一次调用」到「取证闭环」

### 4.1 核心变化：引入 ReAct 循环

以**合规审查 Expert** 为例，对比新旧流程：

**旧（单轮）**
```
规则 + 全文 → LLM → {result, risk_content, advise}
                          ↑ 定位靠后处理字符串匹配
```

**新（ReAct 多轮）**
```
Thought 1: 规则含"不低于100万"，需先找到合同金额
Action 1:  search_document(query="合同总金额 价款", file_id=...)
Observe 1: 命中 3 个块，其中 p.12 有"合同总金额：人民币壹佰贰拾万元整"

Thought 2: 中文大写需转换，且要确认是否为主合同金额而非分项金额
Action 2:  cn_num_to_int("壹佰贰拾万") → 1200000
Action 2b: search_document(query="分项报价 明细", file_id=...)
Observe 2: 分项合计 115 万，与主合同 120 万不一致

Thought 3: 规则要求"不低于100万"，120 万满足；但分项与主合同存在 5 万差异，
           需提示风险
Action 3:  finish(result=true, risk_content=[...], advise="分项合计与主合同
           金额相差 5 万，建议核对")
```

**关键收益**：
- **定位天然准确** —— 风险原文来自检索命中的块，`page_idx`/`bbox` 直接可得，不再依赖字符串匹配
- **可交叉验证** —— Agent 能主动找反例（分项金额、其他章节的同类表述）
- **过程可解释** —— 每一步 Thought/Action 都能落日志，审计友好

### 4.2 各 Expert 的设计要点

| Expert | 自主决策点 | 终止条件 |
|---|---|---|
| **文档解析** | 判断文件类型 → 选解析策略（PDF 直解 / 转 PDF 再解 / 单块合并）；判断是否需 TOC 深度分块 | 产出 middle_json + 分块，或重试 2 次后转人工 |
| **实体抽取** | 先调 `try_rule_check` 做规则抽取 → 未命中字段才调 LLM；判断是否需追加检索 | 目标字段全命中，或检索 3 轮无果 |
| **合规审查** | 规则类型识别 → 取证 → 交叉验证 → 下结论 | 结论置信度达标，或取证 5 轮 |
| **版式检测** | 按检测项选工具；低置信度时换策略复检（如换 dpi 重渲染） | 所有检测项完成，或人工介入 |

### 4.3 规则优先原则（降本关键）

沿用现有 `preprocess` 的设计思路，但在 Agent 中强化：

```
Step 1: match_rule_type(rule)           → 规则能否映射到确定性类型？
Step 2: 若能 → try_rule_based_check()   → 纯代码判定，0 token、0 延迟
Step 3: 若不能 → 才进入 LLM 取证循环
```

预期效果：现有规则库已覆盖 14 类（手机/邮箱/信用代码/身份证/日期/金额/网址/邮编/固话/IP/银行卡/资质有效期…），
**大量常规审查可做到零 LLM 调用**。

---

## 五、L3 编排层：Orchestrator

### 5.1 职责

```python
Orchestrator 的输入：  一个开放式任务 + 文档标识
  "审查这份投标文件，重点关注资质有效性和报价一致性"
  "核对这份合同的印章与签名是否合规"

Orchestrator 的输出：  结构化审查报告
  {
    "summary": "...",
    "findings": [
      {"item": "营业执照", "result": "pass", "evidence": {...}},
      {"item": "报价一致性", "result": "risk", "risk_content": "...",
       "location": {"page_idx": 12, "bbox": [...]}, "advise": "..."}
    ],
    "unresolved": [...]   # 待人工确认
  }
```

### 5.2 规划策略：从固定 DAG 到动态分解

**阶段一（建议先做）：模板化规划**
- 预定义任务模板（投标文件审查 / 合同审查 / 资质审查）
- 每个模板声明需要哪些 Expert、依赖关系、终止条件
- Orchestrator 按模板编排，**但每个 Expert 内部自主决策**

这是风险最低的路径：编排逻辑仍可控，同时获得 Expert 的自主性收益。

**阶段二：LLM 驱动规划**
- 用 LLM 把自然语言任务分解为子任务 DAG
- 引入 `interrupt_before` 让人在关键节点确认
- 适合开放式、长尾任务

### 5.3 共享状态：Blackboard

用 Redis 实现一个跨 Expert 共享的黑板，解决 L4（无记忆）问题：

```python
# 结构
task:{taskId}:blackboard = {
    "entities": {...},        # 已抽取实体（供后续复用，避免重复抽取）
    "findings": [...],        # 已发现的问题
    "evidence": [...],        # 取证记录（含 page_idx / bbox）
    "open_questions": [...]   # 未解决项
}
```

收益：
- 实体只抽一次，多个规则共享（解决 §1.3 的"多规则串行无法共享"问题）
- 支持真正的**跨文档/跨章节一致性**审查（如目录与正文标题比对）

### 5.4 记忆：短期 + 长期

| 类型 | 载体 | 用途 |
|---|---|---|
| 短期（会话内） | `checkpointer`（LangChain 内置） | 多轮 ReAct 的上下文延续 |
| 中期（任务内） | Redis Blackboard | 跨 Expert 共享状态 |
| 长期（跨任务） | Milvus | 历史审查案例 → 相似案例检索（Few-shot 增强） |

> 长期记忆是**可选的高级特性**，建议放到最后做。它的价值在于：相同或相似文档
> 再审时，可以检索历史结论作为参考，减少重复劳动。

---

## 六、实施路线

### 阶段划分（按风险从低到高）

| 阶段 | 目标 | 交付物 | 风险 | 工作量 |
|---|---|---|---|---|
| **P0 准备** | 工具层封装 | `tools/agent_tools/`（5 组 ~40 个工具）+ 单元测试 | 低 | 中 |
| **P1 单点验证** | 合规审查 Expert 试点 | 一个可用的 ReAct Agent + 对比评测 | 中 | 中 |
| **P2 推广** | 其余 3 个 Expert | 完整 L2 层 | 中 | 中 |
| **P3 编排** | Orchestrator + Blackboard | 模板化编排 + 共享状态 | 中高 | 大 |
| **P4 增强** | 长期记忆 + 人机协作 | 案例检索 + `interrupt` 人工确认 | 高 | 大 |

### P0：工具层封装（建议先做这个）

**为什么先做**：纯增量，不改任何现有逻辑，风险最低，且立即产生价值——
即使后续 Agent 不做，这些工具也能复用。

**具体任务**：

1. 新建 `tools/agent_tools/`，按 5 组建文件：
   ```
   tools/agent_tools/
   ├── parse_tools.py      # 解析类 7 个
   ├── retrieval_tools.py  # 检索类 3 个
   ├── validation_tools.py # 校验类 9 个  ← 最高优先级
   ├── detection_tools.py  # 检测类 9 个
   ├── compute_tools.py    # 计算类 3 个
   └── registry.py         # 按 Expert 分组导出
   ```

2. 每个工具配 Pydantic schema + 写清 description（含"何时用"+"结论可信度"）

3. 单元测试：每个工具至少 1 个正例 + 1 个反例

**验收标准**：Agent 能在给定规则时正确选择 `validate_credit_code` 而非自己算校验位。

### P1：合规审查 Expert 试点

**为什么选它**：
- 现有 `run_compliance_audit_agents` 的痛点最明显（§1.3）
- 规则库最完善（14 类），容易验证"规则优先"的收益
- 有明确的评测指标（准确率、召回率）

**实施**：

1. 新建 `agent/compliance_expert.py`：
```python
from langchain.agents import create_agent
from tools.agent_tools.registry import COMPLIANCE_TOOLS

compliance_expert = create_agent(
    model=llm,
    tools=COMPLIANCE_TOOLS,
    system_prompt=load_prompt("compliance_expert_prompt"),
    response_format=ComplianceResult,
    checkpointer=checkpointer,
)

async def audit(rule: str, file_id: str, blackboard: dict):
    # 1. 规则优先：能确定性判定就直接返回
    if quick := try_rule_based_check(rule, blackboard.get("text")):
        return quick
    # 2. 否则进入 ReAct 取证循环
    return await compliance_expert.ainvoke({...})
```

2. **保留旧接口**：`/api/agent/complianceAudit` 内部改为调用 Expert，对外契约不变

**评测方案**（关键，必须做）：

| 指标 | 旧（单轮） | 新（ReAct） | 说明 |
|---|---|---|---|
| 判定准确率 | 基线 | 对比 | 人工标注 100 条用例 |
| 风险定位成功率 | 基线 | 对比 | 能否给出 page_idx/bbox |
| 平均 token 消耗 | 基线 | 对比 | 预期下降（规则优先 + 按需取证） |
| P95 延迟 | 基线 | 对比 | 预期上升（多轮），需权衡 |

> **延迟会上升是必然的**（多轮调用）。如果 P95 不可接受，可限制最大轮次
> （建议 `max_iterations=5`），或对简单规则强制走规则优先路径。

### P2~P4：后续阶段

- **P2**：把 P1 的模式复制到其余 3 个 Expert
- **P3**：Orchestrator + Redis Blackboard，实现跨 Expert 共享
- **P4**：Milvus 案例检索 + `interrupt_before` 人工确认

---

## 七、关键技术点与风险

### 7.1 五个必须处理的技术点

| # | 问题 | 处置 |
|---|---|---|
| **T1** | **工具调用失败** | 用 `handle_tool_error` 自定义错误处理；检测类工具（依赖外部 Paddle 服务）需重试 + 降级 |
| **T2** | **无限循环** | 必须设 `max_iterations`（建议 5~8）+ `recursion_limit`；超时强制终止并返回中间结论 |
| **T3** | **上下文爆炸** | 强制"按需取证"：禁止把整篇文档放进 prompt，一律走 `search_document`；启用消息截断 |
| **T4** | **并发与限流** | 现有 `tools/llm/concurrency.py` 的按模型 Semaphore 需扩展：多轮调用会放大并发压力，建议按"任务"而非"调用"限流 |
| **T5** | **结果不稳定** | `temperature` 保持 0.2；对同一任务可跑 2 次做一致性校验（成本翻倍，仅对高风险项启用） |

### 7.2 风险与缓解

| 风险 | 影响 | 缓解措施 |
|---|---|---|
| **成本上升** | 多轮调用放大 token 消耗 | 规则优先（预计覆盖 60%+ 常规项）+ 按需取证 + 小模型路由（现有 `model_router` 已有） |
| **延迟上升** | 用户体验下降 | 限制轮次；异步化（现有 MQ 架构天然支持）；对实时接口保留旧单轮路径 |
| **行为不可控** | Agent 作出错误决策 | 关键操作（写库、发反馈）加人工确认；所有 Thought/Action 全量落日志 |
| **评测困难** | 无法证明"更好" | 建立固定评测集（建议 100 条标注用例），每次改动前后对比 |
| **改动面大** | 影响现有线上服务 | 严格增量：新逻辑走新接口，验证通过后再切换路由，保留回滚开关 |

### 7.3 一个重要的取舍

**不要追求"完全自主"。** 文档合规审查是**高风险场景**，错误结论可能导致业务损失。

建议的定位是 **"Agent 辅助 + 人工确认"**：
- Agent 负责：取证、检索、初判、定位、生成建议
- 人工负责：最终结论确认、边界case决策

这也符合现有 `interrupt_before` / `ask_human` 的框架能力。

---

## 八、与现有优化项的协同

本方案与技术报告 §12「已知问题」中几个待办事项直接相关：

| 技术报告中的待办 | 本方案如何推进 |
|---|---|
| "实体一致性检查路由：简单走规则、普通走 INT4、复杂走 27B" | Agent 的"规则优先 + 按需升级"是同一思路的自动化版本 |
| "实体提取：规则（主）+ LLM（兜底）" | 直接对应 P1 的规则优先设计 |
| "压缩大模型输入内容长度" | `search_document` 按需取证天然解决（T3） |
| "`tools/` 内小功能分类整理" | P0 的工具封装正好完成这层整理 |

另外一个协同点：本次已修复的**消费者超时/背压问题**（`tools/utils/consumer_callback.py`）
对 Agent 方案**更为关键** —— Agent 单次任务耗时远长于单轮调用，
超时和并发控制的重要性会显著上升。建议：

- `CONSUMER_CALLBACK_TIMEOUT` 需按 Agent 任务重新评估（可能需放宽到 2~3 小时）
- `ENTITY_EXTRACT_MAX_CONCURRENT` 需下调（多轮调用放大 GPU 压力）
- 建议为 Agent 任务**单独建 topic 和消费者**，避免与现有短任务互相挤占

---

## 九、建议的下一步

**立即可做（低风险、高确定性）**：

1. **P0 的 `validation_tools.py`**（9 个校验类工具）
   —— 纯封装、不动业务、立即可用，且能直接验证"规则优先"的收益

2. **建评测集**（100 条标注用例）
   —— 这是后续所有优化的前提，没有基线就无法证明改进

3. **用一个真实场景做 POC**：建议选「资质文件有效期审查」
   —— 规则明确（`_check_validity` 已实现）、痛点明显、易评测

**需要决策的**：

- 是否接受延迟上升换取准确率提升？（影响 P1 的轮次限制设计）
- Agent 结论是直接作为最终结果，还是作为"建议"提交人工？（影响 P3 的流程设计）
- 是否为 Agent 任务单独建 MQ topic？（影响部署架构）

---

## 附录：核心代码骨架

### A. 工具封装示例

```python
# tools/agent_tools/validation_tools.py
from pydantic import BaseModel, Field
from langchain_core.tools import tool
from tools.compliance.rule_patterns import (
    _validate_credit_code, _validate_id_card, _check_validity,
)


class CodeInput(BaseModel):
    code: str = Field(description="待校验的统一社会信用代码，18 位")


@tool("validate_credit_code", args_schema=CodeInput)
def validate_credit_code(code: str) -> dict:
    """校验统一社会信用代码的校验位是否正确（GB32100 标准）。

    何时使用：需要确认某个统一社会信用代码是否合法时。
    结论可信度：确定性的，校验位算法判定，可直接采信。
    注意：只校验编码规则是否合法，不验证该代码是否真实存在。
    """
    return {"valid": _validate_credit_code(code), "code": code}


class ValidityInput(BaseModel):
    text: str = Field(description="含有效期信息的文本，如'有效期至2026年12月31日'")


@tool("check_validity_period", args_schema=ValidityInput)
def check_validity_period(text: str) -> dict:
    """检查资质/证照的有效期是否截至今天仍然有效。

    何时使用：需要判断营业执照、资质证书、许可证等是否过期时。
    结论可信度：日期解析确定，但需确认文本中确实包含有效期信息。
    返回：is_valid / end_date / 说明。若无法提取日期，is_valid 为 None。
    """
    is_valid, end_date, reason = _check_validity(text)
    return {"is_valid": is_valid, "end_date": end_date, "reason": reason}
```

### B. 合规审查 Expert 骨架

```python
# agent/compliance_expert.py
from langchain.agents import create_agent
from langchain_openai import ChatOpenAI
from langchain_core.checkpointer import InMemorySaver
from tools.agent_tools.registry import COMPLIANCE_TOOLS
from tools.compliance.rule_patterns import try_rule_based_check
from loader.prompt_loader import PromptLoader

_prompts = PromptLoader()
_llm = ChatOpenAI(model=..., temperature=0.2, extra_body={
    "chat_template_kwargs": {"enable_thinking": False}})
_checkpointer = InMemorySaver()

MAX_ITERATIONS = 5

expert = create_agent(
    model=_llm,
    tools=COMPLIANCE_TOOLS,
    system_prompt=_prompts.get("compliance_expert_prompt"),
    checkpointer=_checkpointer,
)


async def audit(rule: str, file_id: str, text: str | None = None) -> dict:
    """审查入口：规则优先，LLM 取证兜底。"""
    # 1) 确定性规则优先（0 token）
    if text:
        if quick := try_rule_based_check(rule, text):
            return {"source": "rule", "result": quick}

    # 2) ReAct 取证循环
    result = await expert.ainvoke(
        {"messages": [{"role": "user", "content": f"审查规则：{rule}\n文件：{file_id}"}]},
        config={
            "configurable": {"thread_id": f"{file_id}:{hash(rule)}"},
            "recursion_limit": MAX_ITERATIONS * 2,
        },
    )
    return {"source": "agent", "result": result}
```

### C. 工具分组注册表

```python
# tools/agent_tools/registry.py
from tools.agent_tools.validation_tools import (
    validate_credit_code, validate_id_card, check_validity_period,
    match_rule_type, check_format_by_rule, try_rule_check,
)
from tools.agent_tools.retrieval_tools import search_document, search_knowledge_base

# 合规审查 Expert：校验 + 检索（9+2）
COMPLIANCE_TOOLS = [
    validate_credit_code, validate_id_card, check_validity_period,
    match_rule_type, check_format_by_rule, try_rule_check,
    search_document, search_knowledge_base,
]

# 实体抽取 Expert：校验为主
ENTITY_TOOLS = [
    validate_credit_code, validate_id_card, check_format_by_rule,
    try_rule_check, search_document,
]

# 其余分组见各 Expert 定义
```
